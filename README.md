# vertx-client-example
Vert.x client firing requests against (vertx) server

## Prerequisites

Server is up and running. Listening on port 8082.

## Building

To package your application:
```bash
./gradlew clean assemble
```

To run your application:
```bash
./gradlew clean run
```